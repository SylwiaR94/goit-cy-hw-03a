name: Cypress tests

on:
  push:
    branches:
      - main

jobs:
  cypress-tests-chrome:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Chrome
        uses: SylwiaR94/goit-cy-hw-03@v3
      
      - name: Install Dependensies Chrome
        uses: npm install

      - name: Run Cypress Tests Chrome
        uses: npm run cypress:run:chrome
